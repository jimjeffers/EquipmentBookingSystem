<h1>Editing item</h1>

<% form_for(@item) do |f| %>
  <%= f.error_messages %>
  <ul class="form">
    <%= render :partial => 'item_form', :locals => {:f => f} %>
    <li>
      <%= f.submit 'Update' %>
    </li>
  </ul>
<% end %>

<%= link_to 'Show', @item %> |
<%= link_to 'Back', items_path %>