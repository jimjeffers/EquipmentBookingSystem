<li>
  <%= f.label :name, "Name" %>
  <%= f.text_field :name %>
</li>
<li>
  <%= f.label :category_id, "Parent (optional)" %>
  <%= f.collection_select :category_id, Category.all, :id, :name, {:prompt => true, :include_blank => true} %>
</li>