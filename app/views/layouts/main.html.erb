<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en">
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8">
    <title>EquipmentBooking - <%= controller.controller_name %> - <%= controller.action_name %></title>
    <%= stylesheet_tag "reset" %>
    <%= stylesheet_tag "system" %>
    <%= javascript_tag "var AUTH_TOKEN = #{form_authenticity_token.inspect};" if protect_against_forgery? %>
  </head>
  <body id="<%= @page_id || controller.controller_name %>" class="<%= @page_class || "#{controller.controller_name}_#{controller.action_name}" %>">
    <div class="header">
      <h1 id="uq_equipment_booking">UQ Equipment Booking</h1>
      <%= render_partial 'session/login_form' %>
    </div>
    <% if flash[:notice] %>
    <div id="notice">
      <%= flash[:notice] %>
    </div>
    <% end %>
    <% if flash[:error] %>
    <div id="error">
      <%= flash[:error] %>
    </div>
    <% end %>
    
    <div id="content">
      <%= yield %>
    </div>
  </body>
</html>