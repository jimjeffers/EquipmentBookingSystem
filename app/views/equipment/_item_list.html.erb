<% if !items.blank? %>
  <ol class="item_list">
  <% for item in items %>
    <%= render :partial => 'item_detail_extended', :locals => {:item => item} %>
  <% end %>
  </ol>
<% else %>
  <p>Oops! Sorry no items could be found. Try refining your search!</p>
<% end %>